image: node:alpine

variables:
  PUBLIC_URL: /

cache:
  paths:
    - node_modules

before_script:
  - npm ci

stages:
  - lint
  - deploy

lint:
  stage: lint
  script:
    - npm run lint:gl
  artifacts:
    reports:
      codequality: gl-codequality.json

pages:
  stage: deploy
  script:
    - npm run build
    - mv public _public
    - mv build public
    - cp public/index.html public/404.html
  artifacts:
    paths:
      - public
  only:
    - master
